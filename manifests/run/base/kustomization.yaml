apiVersion: kustomize.config.k8s.io/v1beta1
Kind: Kustomization

namespace: kube-ns-suspender

resources:
  - deployment.yaml
  - rbac.yaml
  - service.yaml
  - service-ui.yaml
  - serviceMonitor.yaml
  - ingress.yaml
